import { Helmet } from 'react-helmet';                                                                                                         

const Title = 'The title you want to see in react app project';
  
return (
        <>
            <Helmet>
                <title>{Title}</title>
               {/*<title>{The title you want to see in react app project}</title>*/}
            </Helmet>
        </>
  )
 
Follow these steps in all react app browser tab and the title will change
To Change the react-app icon in the browser tab just change the file name of image to favicon.ico 
then the icon of react-app will be change...

To install any hook in react js you must write this line of command to install any hook "npm install hook name"
